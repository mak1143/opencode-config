---
description: Generate the project structure
---
Generate the project structure. Create the directory layout, config files, and skeleton code needed to start working. Follow existing conventions in the codebase.

## Steps
- Identify the project type and architecture
- Create the folder structure
- Add config files (tsconfig, package.json, etc.)
- Generate skeleton files with proper imports and exports
