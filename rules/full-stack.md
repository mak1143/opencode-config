---
alwaysApply: true
---

# Full-Stack — Load Learning References When Relevant

When the task involves frontend animation or portfolio work, load `memory/full-stack-frontend.md`:

- GSAP, ScrollTrigger, Lenis, Locomotive Scroll, Barba.js
- Custom cursor, magnetic buttons, page transitions, loading screen
- Portfolio rebuild or redesign work

When the task involves backend, Python, databases, or AI, load `memory/full-stack-backend.md`:

- Python scripts, automation, FastAPI/Flask/Django
- SQL, SQLite, PostgreSQL, SQLAlchemy, migrations
- AI/ML integration (Hugging Face, scikit-learn, model APIs)

Load the file before writing code so the roadmap and conventions shape the implementation. Do not auto-load both on every session — read them on demand.